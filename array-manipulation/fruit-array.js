// Create an array of fruits and display each fruit using a loop.

//forEach->check indivisual array and perform operation in same array

let arr_fruit=["Apple", "Banana", "Mango", "Orange"]
arr_fruit.forEach((element) => {
  console.log(element);
  
});